using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// a skill that will only be unlocked when reaches final trust level. 
/// </summary>
public class FinalSkill
{
    /*// Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public FinalSkill(string n, int pts, float[] speNums) : base(n, pts, speNums) {
    }

    public FinalSkill(string n, float[] speNums) : base(n, speNums) {
    }

    public void AddSkillPts(int pts) {
        if (this.skillPts < 3) {
            this.skillPts += pts;
        }
        
        DoEffect();
    }*/
}
